/**
 * config.js — Local API Key Configuration
 *
 * This file is listed in .gitignore and should NEVER be committed to source control.
 * It is safe to store your local API key here for development only.
 *
 * HOW TO USE:
 * 1. Get a free Gemini API key at: https://aistudio.google.com/
 * 2. Replace null below with your key string:
 *    window.GEMINI_API_KEY = 'AIzaSy...yourkey...';
 * 3. Save and refresh the browser.
 *
 * Without a key, the chatbot uses the built-in local knowledge base (still works great).
 */
window.GEMINI_API_KEY = null; // Replace null with your API key string
